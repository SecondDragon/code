/*
 * Decorator [ˈdekəreɪtə(r)] 装饰器模式 
 * 对象中每个属性都有一个特性值来描述这个属性的特点
 *   + Object.defineProperty(obj, prop, descriptor)
 *   + descriptor
 *     + configurable:是否可以被删除
 *     + writable:是否可以被修改
 *     + enumerable:是否可以被枚举
 *     + value
 *     + get / set
 */